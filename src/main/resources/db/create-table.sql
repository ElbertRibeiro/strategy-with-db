CREATE TABLE IF NOT EXISTS "EXEMPLO"."TAB_VALIDACAO"
(
    "ID"
    NUMBER
    PRIMARY
    KEY
);

CREATE TABLE IF NOT EXISTS "EXEMPLO"."TAB_CARTAO"
(
    "ID"
    NUMBER
    PRIMARY
    KEY,
    "TIPO_CARTAO"
    VARCHAR
(
    50
) );

CREATE TABLE IF NOT EXISTS "EXEMPLO"."TAB_DEBITO"
(
    "ID" NUMBER PRIMARY KEY,
    "DESCRICAO" VARCHAR(50)
);